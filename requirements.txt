# Pinned versions of the environment that produced the committed results
# (Python 3.14, macOS arm64). Regenerate with the helper in the README.
# Abstract ranges live in pyproject.toml; this file is the verified lock.

# --- core ---
hdbscan==0.8.44
joblib==1.5.3
matplotlib==3.11.0
numpy==2.4.6
pandas==3.0.3
pandera==0.32.0
pyarrow==24.0.0
pyyaml==6.0.3
scikit-learn==1.9.0
scipy==1.18.0
seaborn==0.13.2
umap-learn==0.5.12

# --- pipeline ---
dvc==3.67.1

# --- deep ---
dice_ml==0.12
shap==0.52.0
torch==2.11.0

# --- app ---
streamlit==1.60.0

# --- dev ---
black==26.5.1
hypothesis==6.155.7
jupyter==1.1.1
jupytext==1.19.4
nbformat==5.10.4
pre-commit==4.6.0
pytest-cov==7.1.0
pytest==9.1.1
ruff==0.15.19
