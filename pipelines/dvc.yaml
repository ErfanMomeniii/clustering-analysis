# Phase 1 pipeline stages — populated incrementally as each stage is implemented.
# Each stage:
#   - has named deps (code + input data)
#   - has named outs (intermediate or final artefacts)
#   - reads params from params.yaml
#
# After adding a stage, run: dvc repro <stage_name>
stages: {}
