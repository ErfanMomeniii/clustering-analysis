"""End-to-end pipeline test on synthetic blobs.

This stub exists from Task 3 so subsequent tasks have a place to plug their
contributions. The full assertion (ARI > 0.95) is enabled in Task 18 once
every Phase 1 stage is implemented.
"""
import pytest

pytestmark = pytest.mark.slow

def test_pipeline_recovers_three_blobs():
    pytest.skip("Enabled in Task 18 after every Phase 1 stage is implemented.")
